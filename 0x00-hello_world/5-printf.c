#include <stdio.h>

/**
 * main - Main print a message in screen
 *
 * Return: Always 0 (Success)  a 0 returns in the screen
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
